services:
  autocatalogdb:
    image: postgres:17
    
  adservicedb:
    image: postgres:17
    
  redis:
    image: redis:latest
    
  autocatalog.web:
    image: autocatalog.web
    build:
      context: .
      dockerfile: src/Services/AutoCatalog/AutoCatalog.Web/Dockerfile
      
  keycloak:
    image: quay.io/keycloak/keycloak
    
  keycloakdb:
    image: postgres:17
    
  minio:
    image: minio/minio:latest
    
  filemanagement.grpc:
    image: filemanagement.grpc
    build:
      context: .
      dockerfile: src/Services/FileManagement/FileManagement.Grpc/Dockerfile
      
  profileservicedb:
    image: postgres:17
    
  profileservice.api:
    image: profileservice.api
    build:
      context: ./src/Services/ProfileService
      dockerfile: Dockerfile
      args:
        PROFILESERVICE_POSTGRES_URL: postgresql://postgres:1111@profileservicedb:5432/ProfileServiceDb
  
      
      
volumes:
  auto-catalog-postgres:
  ad-service-postgres:
  profile-service-postgres-data:
  keycloak-data:
  minio-data:
    driver: local
  redis-data:
    driver: local
  
  
  

