services:
  autocatalogdb:
    container_name: autocatalogdb
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=1111
      - POSTGRES_DB=AutoCatalogDb
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - "postgres_catalog:/var/lib/postgresql/data/"
      